import React from "react";
import Checkbox from "~/package/components/Checkbox";

const CheckboxPage = () => {
  return (
    <>
      <Checkbox label={"Pick an Option"} />
      <Checkbox label={"Pick an Option"} />
      <Checkbox label={"Pick an Option"} />
    </>
  );
};

export default CheckboxPage;
